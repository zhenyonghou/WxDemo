<view class="container">
    <swiper class="swiper-view" current="{{showIndex}}">
        <block wx:for="{{selectedPhotos}}">
            <swiper-item>
                <image src="{{item}}" class="slide-image"/>
            </swiper-item>
        </block>
    </swiper>
</view>